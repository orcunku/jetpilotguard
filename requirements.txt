# Core runtime dependencies.
# For everything (serving, monitoring, notebooks), use:
#   pip install -e ".[dev,app,mcp,explain,serve,mlops]"
numpy>=1.24,<2.4
pandas>=2.0,<3.0
scikit-learn>=1.3
joblib>=1.3
